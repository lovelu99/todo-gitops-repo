# overlays/dev/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
resources:
  - namespace.yaml                 # ← namespace lives here
  - ingress.yaml                 # ← ingress lives here
  - todo-frontend/kustomization.yaml  # ← frontend resources
  - todo-backend/kustomization.yaml   # ← backend resources