# overlays/prod/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
  - namespace.yaml                 # ← namespace lives here
  - ingress.yaml                 # ← ingress lives here
